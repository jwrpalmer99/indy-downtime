<div class="drep-settings drep-dialog">
  <p class="notes">
    Map custom skill IDs to system skill keys (e.g. <code>persuasion</code> → <code>per</code>).
  </p>

  <div class="drep-alias-list">
    {{#each aliasRows}}
      <div class="drep-alias-row">
        <input type="text" name="aliases.{{@index}}.key" value="{{this.key}}" placeholder="custom id" />
        <input type="text" name="aliases.{{@index}}.value" value="{{this.value}}" placeholder="system key" />
        <button type="button" class="dialog-button" data-drep-action="remove-alias" data-index="{{@index}}">
          Remove
        </button>
      </div>
    {{/each}}
  </div>

  <div class="form-group">
    <button type="button" class="dialog-button" data-drep-action="add-alias">
      Add Mapping
    </button>
  </div>

  <footer class="sheet-footer flexrow">
    <button type="submit" class="dialog-button">
      <i class="far fa-save"></i>
      Save
    </button>
  </footer>
</div>
