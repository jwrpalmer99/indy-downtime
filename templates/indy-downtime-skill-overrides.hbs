<div class="drep-settings" autocomplete="off">
  <div class="form-group">
    <label>Skills (one per line, key or key: label)</label>
    <textarea name="skillsText" rows="8" class="drep-json">{{skillsText}}</textarea>
  </div>

  <div class="form-group">
    <label>Abilities (one per line, key or key: label)</label>
    <textarea name="abilitiesText" rows="6" class="drep-json">{{abilitiesText}}</textarea>
  </div>

  <p class="notes">Overrides the detected skill and ability list for system-agnostic sheets.</p>

  <footer class="sheet-footer flexrow">
    <button type="submit" class="dialog-button">
      <i class="far fa-save"></i>
      Save Overrides
    </button>
  </footer>
</div>
